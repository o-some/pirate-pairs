// Final B04 QA: reviewed ordering + full Varkos smoke.
import { test, expect } from '@playwright/test';
const BASE='http://localhost:4321/pirate-pairs/';
test.use({viewport:{width:390,height:844},deviceScaleFactor:3,isMobile:true,hasTouch:true});
const pairKey=id=>String(id||'').replace(/-(source|target)$/,'');
async function waitPlayer(page){await expect.poll(async()=>{const turn=(await page.locator('#turnPill').textContent())||'';const peekDisabled=await page.locator('#peekBtn').isDisabled();const peekText=(await page.locator('#peekBtn').textContent())||'';const bannerClass=(await page.locator('#bossAbilityBanner').getAttribute('class'))||'';const transient=await page.locator('#grid .card.boss-swap-a,#grid .card.boss-swap-b,#grid .card.corvin-shifting,#grid .card.bomb-targeting,#grid .card.chain-forming').count();const faceUp=await page.locator('#grid .card.flipped:not(.matched)').count();return turn==='DU BIST DRAN'&&!bannerClass.includes('show')&&transient===0&&faceUp===0&&(!peekDisabled||peekText.includes('VERBRAUCHT'));},{timeout:20000}).toBe(true);}
async function startVarkos(page){await page.goto(`${BASE}?boss=10`,{waitUntil:'networkidle'});await expect(page.locator('#grid .card')).toHaveCount(16);await expect(page.locator('#bossName')).toContainText('Varkos');await expect(page.locator('#introBossSprite')).toHaveAttribute('src',/boss-10-piratenkoenig-varkos\.png/);await page.locator('#startBtn').tap();await expect(page.locator('#intro')).toHaveClass(/hidden/);await waitPlayer(page);await expect(page.locator('body')).toHaveAttribute('data-varkos-phase','1');await expect(page.locator('.varkos-phase-inline')).toContainText('PHASE I');}
async function playableCards(page,{excludeBomb=false}={}){const selector='#grid .card:not(.matched):not(.fogged):not(.chained):not(.shadowed)';return page.locator(selector).evaluateAll((nodes,opts)=>nodes.map(n=>({id:n.dataset.id,idx:Number(n.dataset.index),flipped:n.classList.contains('flipped'),bomb:n.classList.contains('bomb-armed')})).filter(x=>!x.flipped&&(!opts.excludeBomb||!x.bomb)),{excludeBomb});}
async function findPair(page,options={}){const list=await playableCards(page,options),groups=new Map();for(const card of list){const key=pairKey(card.id);if(!groups.has(key))groups.set(key,[]);groups.get(key).push(card);}return [...groups.values()].find(group=>group.length===2);}
async function findMismatch(page){const list=await playableCards(page);for(let i=0;i<list.length;i++)for(let j=i+1;j<list.length;j++)if(pairKey(list[i].id)!==pairKey(list[j].id))return [list[i],list[j]];return null;}
async function progress(page){const text=(await page.locator('#progress').textContent())||'0 / 8';return Number(text.split('/')[0].trim());}
async function matchPair(page,{excludeBomb=false,waitForPlayer=true}={}){await waitPlayer(page);const p=await findPair(page,{excludeBomb});expect(p,'need a playable translation pair').toBeTruthy();const before=await progress(page);await page.locator(`#grid .card[data-index="${p[0].idx}"]`).tap();await page.locator(`#grid .card[data-index="${p[1].idx}"]`).tap();await expect.poll(()=>progress(page),{timeout:10000}).toBe(before+1);if(waitForPlayer&&before+1<8)await waitPlayer(page);return p;}
async function ids(page){return page.locator('#grid .card').evaluateAll(ns=>ns.map(n=>n.dataset.id));}
test('Boss Mechanics B04: Piratenkönig Varkos three-phase fight',async({page})=>{test.setTimeout(300000);const errors=[];page.on('console',m=>{if(m.type()==='error')errors.push(m.text())});await startVarkos(page);await matchPair(page);const mismatch=await findMismatch(page);expect(mismatch).toBeTruthy();await page.evaluate(()=>{window.__varkosQaOrder=[];const banner=document.querySelector('#bossAbilityBanner'),title=document.querySelector('#bossAbilityTitle'),turn=document.querySelector('#turnPill');const record=()=>{const events=window.__varkosQaOrder;if(banner?.classList.contains('show')&&/VARKOS VERSCHIEBT/.test(title?.textContent||'')&&!events.includes('effect'))events.push('effect');if(/VARKOS DENKT/.test(turn?.textContent||'')&&!events.includes('ai'))events.push('ai');};const observer=new MutationObserver(record);observer.observe(banner,{attributes:true,subtree:true,childList:true,characterData:true});observer.observe(title,{subtree:true,childList:true,characterData:true});observer.observe(turn,{subtree:true,childList:true,characterData:true});window.__varkosQaObserver=observer;record();});await page.locator(`#grid .card[data-index="${mismatch[0].idx}"]`).tap();await page.locator(`#grid .card[data-index="${mismatch[1].idx}"]`).tap();await expect.poll(()=>page.evaluate(()=>window.__varkosQaOrder||[]),{timeout:12000}).toContain('effect');await expect.poll(()=>page.evaluate(()=>window.__varkosQaOrder||[]),{timeout:12000}).toContain('ai');const order=await page.evaluate(()=>window.__varkosQaOrder||[]);expect(order.indexOf('effect')).toBeLessThan(order.indexOf('ai'));await page.evaluate(()=>window.__varkosQaObserver?.disconnect());await page.locator('#restartBtn').evaluate(el=>el.click());await waitPlayer(page);await matchPair(page);const phase1Before=await ids(page);await matchPair(page);const phase1After=await ids(page);expect(phase1After).not.toEqual(phase1Before);await matchPair(page);await expect(page.locator('body')).toHaveAttribute('data-varkos-phase','2');await expect(page.locator('#grid .card.bomb-armed')).toHaveCount(1,{timeout:6000});const bomb=page.locator('#grid .card.bomb-armed').first();const bombId=await bomb.getAttribute('data-id');const bombIdx=Number(await bomb.getAttribute('data-index'));const all=await playableCards(page);const mate=all.find(c=>c.idx!==bombIdx&&pairKey(c.id)===pairKey(bombId));expect(mate).toBeTruthy();const playerBeforeBomb=Number(await page.locator('#playerScore').textContent()),bossBeforeBomb=Number(await page.locator('#aiScore').textContent()),progressBeforeBomb=await progress(page);await page.locator(`#grid .card[data-index="${bombIdx}"]`).tap();await expect.poll(async()=>Number(await page.locator('#aiScore').textContent()),{timeout:5000}).toBe(bossBeforeBomb+1);await page.locator(`#grid .card[data-index="${mate.idx}"]`).tap();await expect.poll(()=>progress(page),{timeout:10000}).toBe(progressBeforeBomb+1);await waitPlayer(page);expect(Number(await page.locator('#playerScore').textContent())).toBe(playerBeforeBomb);await expect(page.locator('#grid .card.chained')).toHaveCount(2,{timeout:5000});const phase3Before=await ids(page);await matchPair(page);const phase3After=await ids(page);await expect(page.locator('body')).toHaveAttribute('data-varkos-phase','3');expect(phase3After).not.toEqual(phase3Before);const p=await findPair(page);expect(p).toBeTruthy();await page.locator(`#grid .card[data-index="${p[0].idx}"]`).tap();await page.locator(`#grid .card[data-index="${p[1].idx}"]`).tap();await expect.poll(async()=>await page.locator('#bossAbilityTitle').textContent(),{timeout:7000}).toMatch(/VARKOS ZERREISST/);await page.locator('#restartBtn').evaluate(el=>el.click());await waitPlayer(page);const restartedIds=await ids(page);await page.waitForTimeout(1900);expect(await ids(page)).toEqual(restartedIds);await page.locator('#peekBtn').tap();await expect(page.locator('#grid .card.peek')).toHaveCount(2);await page.waitForTimeout(1450);await expect(page.locator('#grid .card.peek')).toHaveCount(0);while(await progress(page)<8){const before=await progress(page);await matchPair(page,{excludeBomb:true,waitForPlayer:before!==7});}await expect(page.locator('#result')).not.toHaveClass(/hidden/,{timeout:6000});await expect(page.locator('#resultTag')).toContainText('PIRATENKÖNIG BESIEGT');await expect(page.locator('#resultModalNote')).toContainText('ALLE 10 BOSSE BESIEGT');expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth+1)).toBeTruthy();expect(errors).toEqual([]);});
